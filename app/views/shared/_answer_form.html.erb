<%= form_for(answer) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.hidden_field :question_id %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new answer.." %>
  </div>
  <%= f.submit "Post Answer", class: "btn btn-black" %>
<% end %>
